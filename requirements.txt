# Deep Learning from Scratch — Dependencies
# Install: pip install -r requirements.txt

# Core (notebooks 01, 02, 04, 07, 08, 09, birth_of_a_neuron)
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
h5py>=3.9
tqdm>=4.65
nbimporter>=0.3.1

# birth_of_a_neuron (3D viz)
plotly>=5.18
kaleido>=0.2.1

# Notebooks 11 & 12 (MNIST, CNN)
tensorflow>=2.14
seaborn>=0.12
