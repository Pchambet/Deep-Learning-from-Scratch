# Deep Learning from Scratch — Dependencies
# Install: pip install -r requirements.txt

# Supported Python versions
# - Core stack: 3.10+
# - TensorFlow stack (MNIST/CNN): 3.10–3.12 recommended

# Testing
pytest>=7.0

# Core (notebooks 01, 02, 04, 07, 08, 09, birth_of_a_neuron)
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
h5py>=3.9
tqdm>=4.65
nbimporter>=0.3.1
jupyter>=1.0
nbconvert>=7.10

# birth_of_a_neuron (3D viz)
plotly>=5.18
kaleido>=0.2.1

# Notebooks 11 & 12 (MNIST, CNN)
# Linux/Windows
tensorflow>=2.16,<2.21; python_version < "3.13" and platform_system != "Darwin"
# macOS (Apple Silicon / Metal)
tensorflow-macos>=2.16,<2.21; python_version < "3.13" and platform_system == "Darwin"
tensorflow-metal>=1.1; python_version < "3.13" and platform_system == "Darwin" and platform_machine == "arm64"

seaborn>=0.12
